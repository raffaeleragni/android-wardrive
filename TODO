
 - Display AP info in someway.

 - Add sound when discovering new networks, or updating position/data

[WEB]
Things to do when appengine gets better:
 
 - queries with lat+lon directly
 
 - query to fetch only new data (to sync on the phone)